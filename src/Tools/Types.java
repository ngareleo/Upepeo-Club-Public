package Tools;

public class Types {
    public enum QueryProgress {NOT_STARTED, ERROR, COMPLETE};
    public enum VideoCategory {GENERAL, COMEDY, HORROR, CARTOON, THRILLER};
    public enum VideoStatus {AVAILABLE, WITHDRAWN, LOST, BORROWED};
    public enum Genders {MALE, FEMALE, OTHERS, RATHER_NOT_SAY, NULL};
    public enum TransactionTypes {MEMBER_REGISTRATION, BORROWING_FEE, FINE_FEE};
}

